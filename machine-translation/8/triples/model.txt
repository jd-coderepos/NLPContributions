(Contribution||has||Model)
(Model||introduce||extension to the encoder-decoder model)
(extension to the encoder-decoder model||learns||align and translate jointly)
(Model||(soft-)searches||a set of positions)
(a set of positions||associated with||context vectors)
(context vectors||predicts||target word)
(target word||by||model)
(a set of positions||in||source sentence)
(a set of positions||where||most relevant information is concentrated)
(Model||(soft-)searches||each time)
(each time||generates||word in a translation)
