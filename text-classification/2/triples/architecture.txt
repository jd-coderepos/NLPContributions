(Contribution||has||Architecture)
(Architecture||use||hierarchical softmax (Goodman, 2001))
(hierarchical softmax (Goodman, 2001)||based on||Huffman coding tree (Mikolov et al., 2013))
(Architecture||use||softmax function f)
(softmax function f||to compute||probability distribution)
(probability distribution||over||predefined classes)
(Architecture||use||bag of n-grams)
(bag of n-grams||to capture||some partial information about the local word order)
(some partial information about the local word order||achieving||comparable results)
(comparable results||to||methods that explicitly use the order (Wang and Manning, 2012))
(some partial information about the local word order||in practice||very efficient)
(Architecture||maintain||fast and memory efficient mapping)
(fast and memory efficient mapping||of||n-grams)
(n-grams||using||hashing trick (Weinberger et al., 2009))
(hashing trick (Weinberger et al., 2009)||with||same hashing function as in Mikolov et al. (2011))
(hashing trick (Weinberger et al., 2009)||with||10M bins)
(10M bins||only used||bigrams)
(Architecture||averaged||word representations)
(word representations||into||text representation)
(text representation||fed to||linear classifier)
(Architecture||has||simple linear model with rank constraint)
