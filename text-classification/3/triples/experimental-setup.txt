(Contribution||has||Experimental setup)
(Experimental setup||tried||two classification models)
(two classification models||first||standard CNN model)
(standard CNN model||using||ReLU (Nair and Hinton, 2010) as non-linear activation function)
(two classification models||second||model)
(model||add||recurrent layer (specifically an LSTM (Hochreiter and Schmidhuber, 1997)))
(recurrent layer (specifically an LSTM (Hochreiter and Schmidhuber, 1997))||before passing||pooled features directly to the fully connected softmax layers)
(Experimental setup||initialized||embedding layer)
(embedding layer||using||300-dimensional CBOW Word2vec embeddings (Mikolov et al., 2013a))
(300-dimensional CBOW Word2vec embeddings (Mikolov et al., 2013a)||trained on||3B-word UMBC WebBase corpus (Han et al., 2013))
(3B-word UMBC WebBase corpus (Han et al., 2013)||with||standard hyperparameters)
